version: 1
frontend:
  phases:
    preBuild:
      commands:
        - PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true npm ci
    build:
      commands:
        - npm run build
    postBuild:
      commands:
        - rm -rf .next/cache
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
